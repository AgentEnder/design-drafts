{
  "name": "@design-drafts/docs",
  "version": "0.0.0",
  "private": true,
  "description": "The docs site at the root of the published Pages site, rendered by design-drafts itself.",
  "type": "module",
  "scripts": {
    "build": "design-drafts build content --out dist --base \"${DOCS_BASE:-/}\" --site-name design-drafts-docs --force && node tools/bundle-overlays.mjs"
  },
  "devDependencies": {
    "@design-drafts/annotate": "workspace:*",
    "@design-drafts/cli": "workspace:*",
    "@design-drafts/toolbar": "workspace:*"
  },
  "nx": {
    "name": "docs",
    "targets": {
      "build": {
        "outputs": [
          "{projectRoot}/dist"
        ],
        "inputs": [
          "production",
          "^production",
          {
            "env": "DOCS_BASE"
          }
        ]
      }
    }
  }
}
