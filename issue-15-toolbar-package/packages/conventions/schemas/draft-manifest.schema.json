{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://design-drafts.dev/schemas/draft-manifest.schema.json",
  "title": "DraftManifest",
  "description": "Machine-readable description of a design draft. Lives at the root of a draft directory as draft.config.json. Themes, layouts, pages, and variants are all distinct HTML files; the manifest is a routing table consumed by the toolbar, the index site, and the annotate package.",
  "additionalProperties": false,
  "type": "object",
  "required": [
    "name",
    "createdAt"
  ],
  "properties": {
    "$schema": {
      "description": "Optional JSON Schema reference for editor tooling.",
      "type": "string"
    },
    "name": {
      "minLength": 1,
      "description": "Human-readable label for the draft.",
      "type": "string"
    },
    "description": {
      "description": "Longer explanation of what the draft is exploring.",
      "type": "string"
    },
    "pages": {
      "description": "Distinct pages (e.g. landing, pricing) that exist in this draft. Each path points at an HTML file relative to the draft root.",
      "type": "array",
      "items": {
        "additionalProperties": false,
        "type": "object",
        "required": [
          "name",
          "path"
        ],
        "properties": {
          "name": {
            "minLength": 1,
            "description": "Human-readable label for the page.",
            "type": "string"
          },
          "path": {
            "pattern": "^(?!\\/)(?!.*\\.\\.(\\/|$)).+$",
            "minLength": 1,
            "description": "Path to an HTML file, relative to the draft root. Must not be empty or absolute and must not escape the draft root.",
            "type": "string"
          }
        }
      }
    },
    "variants": {
      "description": "Alternative HTML files representing different variants of the design.",
      "type": "array",
      "items": {
        "additionalProperties": false,
        "type": "object",
        "required": [
          "name",
          "path"
        ],
        "properties": {
          "name": {
            "minLength": 1,
            "description": "Human-readable label for the entry.",
            "type": "string"
          },
          "path": {
            "pattern": "^(?!\\/)(?!.*\\.\\.(\\/|$)).+$",
            "minLength": 1,
            "description": "Path to an HTML file, relative to the draft root. Must not be empty or absolute and must not escape the draft root.",
            "type": "string"
          },
          "description": {
            "description": "Optional longer description shown in switchers.",
            "type": "string"
          }
        }
      }
    },
    "themes": {
      "description": "Theme variations, each as a distinct HTML file.",
      "type": "array",
      "items": {
        "additionalProperties": false,
        "type": "object",
        "required": [
          "name",
          "path"
        ],
        "properties": {
          "name": {
            "minLength": 1,
            "description": "Human-readable label for the entry.",
            "type": "string"
          },
          "path": {
            "pattern": "^(?!\\/)(?!.*\\.\\.(\\/|$)).+$",
            "minLength": 1,
            "description": "Path to an HTML file, relative to the draft root. Must not be empty or absolute and must not escape the draft root.",
            "type": "string"
          },
          "description": {
            "description": "Optional longer description shown in switchers.",
            "type": "string"
          }
        }
      }
    },
    "layouts": {
      "description": "Layout variations, each as a distinct HTML file.",
      "type": "array",
      "items": {
        "additionalProperties": false,
        "type": "object",
        "required": [
          "name",
          "path"
        ],
        "properties": {
          "name": {
            "minLength": 1,
            "description": "Human-readable label for the entry.",
            "type": "string"
          },
          "path": {
            "pattern": "^(?!\\/)(?!.*\\.\\.(\\/|$)).+$",
            "minLength": 1,
            "description": "Path to an HTML file, relative to the draft root. Must not be empty or absolute and must not escape the draft root.",
            "type": "string"
          },
          "description": {
            "description": "Optional longer description shown in switchers.",
            "type": "string"
          }
        }
      }
    },
    "prompt": {
      "description": "The brief that generated the draft. Free text, or a path reference like 'references/brief.md'.",
      "type": "string"
    },
    "source": {
      "additionalProperties": false,
      "description": "Snapshot of where the draft came from.",
      "type": "object",
      "properties": {
        "sha": {
          "minLength": 1,
          "description": "Git commit SHA the draft was generated from.",
          "type": "string"
        },
        "repo": {
          "minLength": 1,
          "description": "Repository identifier (e.g. owner/name or URL).",
          "type": "string"
        },
        "author": {
          "minLength": 1,
          "description": "Author or agent that produced the draft.",
          "type": "string"
        }
      }
    },
    "createdAt": {
      "format": "date-time",
      "description": "ISO 8601 timestamp marking when the draft was generated.",
      "type": "string"
    }
  }
}
